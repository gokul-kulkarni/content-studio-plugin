---
name: write-substack
description: "Writing phase for Substack — draft a publish-ready long-form essay from the LOCKED master content, in Substack's native reading-and-email format. Use whenever someone wants to write a Substack post, essay, article, or newsletter, or turn a finalized idea into long-form prose. Substack rewards clarity and scannability, not visual tricks. Do not run until the content is locked and approved (see explore); to fix an existing rough draft, use refine."
---

# Write — Substack

Turn the **locked master content** for **$ARGUMENTS** into a long-form essay. Substack is a *reading-and-email* platform — most reads happen in the email client, and the enemy is the wall of text.

## Preconditions & change protocol (read first)

- **Content first.** Only render once the master content is locked and the person has approved it (Phase 1 / **explore**). If it isn't, stop and finalize the content first.
- **One spine, faithful rendering.** This is the same thesis as the LinkedIn carousel and Medium article, rendered as prose — it reinforces, never duplicates. Never paste carousel slides in as essay sections or images.
- **Same running example** as every other asset.
- **Change protocol.** If the person asks to change the substance after this exists, don't patch it here — update the master content (**explore**), re-approve, and regenerate all platform assets (**refine** runs the re-sync).
- **Match the person's voice** — preserve first-person reflection, distinctive phrasing, and their own metaphors.

Save the essay as a Markdown file.

## Structure

- **Title** (`#`) — the strongest framing of the thesis.
- **Subtitle** (`####`) — one line naming the specific, non-obvious shift the piece reveals (the curiosity gap).
- **Short hook opening** — 1–2 tight paragraphs dropping the reader into a concrete moment, then a one-line pivot into the body ("Then we started climbing.").
- **Sectioned body with `##` headers** on every major beat — the single biggest fix for "people lose interest"; headers give the eye landing spots.
- **Bold lead-ins** for enumerated history or steps, so the section scans even when skimmed (e.g. **"Java — goodbye to the machine."**).
- **A blockquote** (`>`) around the single most quotable line — the core mental model — so it pops and is screenshot-able. Screenshot-ability is how essays get pulled back onto LinkedIn/X by other people.
- **Numbered list** for any explicit progression of steps.
- **A short, open-ended ender** — ideally a two-line kicker that hands the question back to the reader. If the person already wrote a strong ending, keep it.
- **Series continuity** — if part of a series, open by recalling where the previous piece left off and close by teasing the next.

## Notes

- Keep it scannable: a reader skimming only the title, subtitle, headers, and bold lead-ins should still get the whole arc.
- **Images are uploaded manually** into the Substack editor at each section spot — the Markdown references (e.g. `light-assets/2-...png`) are just pointers. Keep Substack light on images (email rendering + deliverability): a shared hero plus a few section diagrams, not one per paragraph.
- The **SEO/meta description, topic categories, launch Note, and posting** all live in the distribute skill — point there once the essay is drafted.
- For the per-section image specs, use the **design-brief** skill once the content is locked.
- If the input is an existing rough or overlong draft rather than locked content, use the refine skill instead (restructure, keep ideas).
